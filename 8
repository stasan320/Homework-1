#include <iostream>
using namespace std;
int proper_fraction (int n) {
    int number=0;
    for (int i=1;i<n;i++) {
        if (n%i!=0) number++;
    }
    return number*2;
}
int main () {
int n;
cin>>n;
cout<<proper_fraction(n);
}
